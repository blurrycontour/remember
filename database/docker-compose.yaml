version: '3.8'
services:
  mongodb:
    image: mongo:latest
    restart: unless-stopped
    container_name: mongodb
    ports:
      - 9211:27017
    volumes:
      -  ./data:/data/db
#     volumes:
#       - dbdata6:/data/db
# volumes:
#   dbdata6:
